#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../fibrous/bin/fibrous" "$@"
  ret=$?
else 
  node  "$basedir/../fibrous/bin/fibrous" "$@"
  ret=$?
fi
exit $ret
